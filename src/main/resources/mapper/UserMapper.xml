<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.blog.dao.UserDao">
    
    <resultMap id="UserResultMap" type="com.blog.entity.User">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="email" column="email"/>
        <result property="nickname" column="nickname"/>
        <result property="avatar" column="avatar"/>
        <result property="role" column="role"/>
        <result property="status" column="status"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>
    
    <select id="findByUsername" resultMap="UserResultMap">
        SELECT * FROM user WHERE username = #{username}
    </select>
    
    <select id="findById" resultMap="UserResultMap">
        SELECT * FROM user WHERE id = #{id}
    </select>
    
    <select id="findAll" resultMap="UserResultMap">
        SELECT * FROM user ORDER BY create_time DESC
    </select>
    
    <select id="findByPage" resultMap="UserResultMap">
        SELECT * FROM user ORDER BY create_time DESC LIMIT #{offset}, #{limit}
    </select>
    
    <select id="countAll" resultType="long">
        SELECT COUNT(*) FROM user
    </select>
    
    <insert id="insert" parameterType="com.blog.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user (username, password, email, nickname, avatar, role, status)
        VALUES (#{username}, #{password}, #{email}, #{nickname}, #{avatar}, #{role}, #{status})
    </insert>
    
    <update id="update" parameterType="com.blog.entity.User">
        UPDATE user SET
            username = #{username},
            password = #{password},
            email = #{email},
            nickname = #{nickname},
            avatar = #{avatar},
            role = #{role},
            status = #{status}
        WHERE id = #{id}
    </update>
    
    <delete id="deleteById">
        DELETE FROM user WHERE id = #{id}
    </delete>
    
    <update id="updateStatus">
        UPDATE user SET status = #{status} WHERE id = #{id}
    </update>
    
</mapper>