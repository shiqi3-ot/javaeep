<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.blog.dao.CategoryDao">
    
    <resultMap id="CategoryResultMap" type="com.blog.entity.Category">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="description" column="description"/>
        <result property="sortOrder" column="sort_order"/>
        <result property="createTime" column="create_time"/>
    </resultMap>
    
    <select id="findById" resultMap="CategoryResultMap">
        SELECT * FROM category WHERE id = #{id}
    </select>
    
    <select id="findAll" resultMap="CategoryResultMap">
        SELECT * FROM category ORDER BY create_time DESC
    </select>
    
    <select id="findAllOrderBySort" resultMap="CategoryResultMap">
        SELECT * FROM category ORDER BY sort_order ASC, create_time ASC
    </select>
    
    <insert id="insert" parameterType="com.blog.entity.Category" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO category (name, description, sort_order)
        VALUES (#{name}, #{description}, #{sortOrder})
    </insert>
    
    <update id="update" parameterType="com.blog.entity.Category">
        UPDATE category SET
            name = #{name},
            description = #{description},
            sort_order = #{sortOrder}
        WHERE id = #{id}
    </update>
    
    <delete id="deleteById">
        DELETE FROM category WHERE id = #{id}
    </delete>
    
</mapper>